# Secrets files - never commit these
*.secrets
.zshrc.secrets
.zshrc.local

# Environment files with secrets
.env
.env.local
*.env.local

# Homebrew bundle lockfile (this repo tracks desired packages, not pins)
Brewfile.lock.json
